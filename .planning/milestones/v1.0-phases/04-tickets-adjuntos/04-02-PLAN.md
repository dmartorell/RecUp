---
phase: 04-tickets-adjuntos
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/js/ticket-modal.js
  - src/js/attachments.js
  - src/js/app.js
  - src/index.html
  - src/css/styles.css
autonomous: false
requirements: [TICK-02, TICK-03, ADJU-01, ADJU-02, RESU-03]

must_haves:
  truths:
    - "El usuario puede editar el titulo generado por Claude antes de crear el ticket"
    - "El usuario puede rellenar campos metadata (reportedBy, affectedUser, projectId, assetId) en el modal"
    - "El usuario puede adjuntar fotos/videos desde galeria o camara y ver preview"
    - "Al crear el ticket, el usuario ve progress bar y recibe confirmacion con enlace a ClickUp"
    - "Si los adjuntos fallan pero el ticket se creo, el usuario ve error parcial con opcion de reintentar"
    - "La card se marca como enviada con badge verde tras crear el ticket"
  artifacts:
    - path: "src/js/ticket-modal.js"
      provides: "Modal de creacion de ticket con formulario y logica de envio"
      exports: ["openTicketModal"]
    - path: "src/js/attachments.js"
      provides: "Selector de archivos con preview y validacion"
      exports: ["AttachmentManager"]
    - path: "src/index.html"
      provides: "Markup del modal"
      contains: "ticket-modal"
  key_links:
    - from: "src/js/app.js"
      to: "src/js/ticket-modal.js"
      via: "import openTicketModal, click handler en boton Crear Ticket"
      pattern: "import.*openTicketModal"
    - from: "src/js/ticket-modal.js"
      to: "/api/ticket"
      via: "fetch POST JSON"
      pattern: "fetch.*api/ticket"
    - from: "src/js/ticket-modal.js"
      to: "/api/attachment"
      via: "fetch POST FormData"
      pattern: "fetch.*api/attachment"
    - from: "src/js/ticket-modal.js"
      to: "src/js/attachments.js"
      via: "import AttachmentManager para gestionar archivos"
      pattern: "import.*AttachmentManager"
    - from: "src/js/app.js"
      to: "card.dataset.summary*"
      via: "Lee datos del resumen guardados en dataset para prellenar modal"
      pattern: "dataset\\.summary"
---

<objective>
Modal de creacion de ticket con campos editables, adjuntos con preview, y flujo completo de envio con feedback visual.

Purpose: Conectar el frontend con los endpoints backend para completar el flujo audio-to-ticket. El usuario graba, Claude resume, y con un click crea un ticket en ClickUp con evidencia adjunta.
Output: Modal funcional, modulo de adjuntos, integracion completa en app.js.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tickets-adjuntos/04-CONTEXT.md
@.planning/phases/04-tickets-adjuntos/04-RESEARCH.md
@.planning/phases/04-tickets-adjuntos/04-01-SUMMARY.md

<interfaces>
<!-- Datos del resumen guardados en card.dataset por app.js (Phase 3) -->
card.dataset.summaryTitle    // string — titulo generado por Claude
card.dataset.summaryTranscript  // string — transcripcion limpia
card.dataset.summaryBullets  // JSON string — array de bullets

<!-- Endpoints creados en Plan 01 -->
POST /api/ticket
  Body: { name, markdown_description, reportedBy, affectedUser, projectId, assetId }
  Response: { id, url }

POST /api/attachment
  Body: FormData con campo 'taskId' + campos 'attachment' (archivos)
  Response: { attachments: [...] }
  Error parcial: { error, file, uploaded }

<!-- Funciones existentes en app.js -->
function showToast(message)  // Notificacion toast
function createCard(transcript, audioBlob, duration)  // Crea card en feed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modulo attachments.js con selector de archivos y preview</name>
  <files>src/js/attachments.js</files>
  <action>
Crear `src/js/attachments.js` con clase `AttachmentManager`:

```
export class AttachmentManager {
  constructor(containerEl) — recibe el elemento DOM donde renderizar previews

  getFiles() — devuelve array de File objects actuales
  getCount() — devuelve numero de archivos
  clear() — limpia todos los archivos y previews
  addFiles(fileList) — anade archivos desde un input.files, con validacion
  removeFile(index) — quita un archivo por indice y actualiza previews
  render() — renderiza grid de previews en containerEl
}
```

Validaciones en addFiles:
- Maximo 5 archivos total (sumando los ya existentes). Si se excede, NO anadir y devolver string de error.
- Maximo 10MB por archivo. Si alguno excede, NO anadir ese archivo y devolver string de error.
- Solo aceptar image/* y video/*. Rechazar otros tipos con error.

Preview con `URL.createObjectURL()`:
- Imagenes: `<img>` con clase para thumbnail (object-cover, rounded, h-20)
- Videos: `<video>` sin controls, mismas clases
- Cada preview tiene boton X para eliminar (llama removeFile)
- Cada preview muestra nombre del archivo truncado
- Llamar `URL.revokeObjectURL()` en removeFile y clear para liberar memoria

Exportar la clase para importarla desde ticket-modal.js.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/_ALFRED/Bugshot && grep -q "class AttachmentManager" src/js/attachments.js && grep -q "revokeObjectURL" src/js/attachments.js && grep -q "export" src/js/attachments.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>AttachmentManager exportado con gestion de archivos, validacion de limites (5 archivos, 10MB, image/video), y preview con thumbnails eliminables</done>
</task>

<task type="auto">
  <name>Task 2: Modal de creacion de ticket con formulario y flujo de envio</name>
  <files>src/js/ticket-modal.js, src/index.html, src/css/styles.css</files>
  <action>
1. En `src/index.html`, anadir markup del modal ANTES del cierre de `</body>`:
   - Overlay con id="ticket-modal" (hidden por defecto, z-40)
   - Panel centrado con max-w-lg, fondo blanco, rounded, shadow
   - Header: "Crear Ticket" + boton cerrar (X)
   - Formulario con campos:
     - Titulo (input text, id="ticket-title", editable, prellenado — RESU-03)
     - Descripcion (textarea readonly, id="ticket-description", muestra bullets como texto, NO editable per CONTEXT.md)
     - Reportado por (input text, id="ticket-reported-by")
     - Usuario afectado (input text, id="ticket-affected-user")
     - Project ID (input text, id="ticket-project-id")
     - Asset ID (input text, id="ticket-asset-id")
   - Seccion adjuntos:
     - Dos botones: "Anadir archivo" (input file hidden, accept="image/*,video/*", multiple) y "Hacer foto" (input file hidden, accept="image/*", capture="environment")
     - Container id="attachment-previews" para el grid de previews
   - Footer:
     - Progress bar (hidden por defecto, id="ticket-progress")
     - Boton "Crear Ticket" (id="ticket-submit-btn", primary style)
     - Boton "Cancelar" (secondary style)
   Usar Tailwind classes para todo el layout. Estilos custom minimos en styles.css.

2. En `src/css/styles.css`, anadir estilos para:
   - .modal-overlay: fixed inset-0, bg negro semitransparente, flex center
   - .modal-panel: animacion de entrada (scale/opacity)
   - .progress-bar: height 4px, bg-accent, transition width
   - .badge-sent: verde, para cards enviadas
   - .attachment-grid: grid 3 cols, gap
   - .attachment-thumb: object-cover, rounded, con boton X superpuesto

3. Crear `src/js/ticket-modal.js`:
   - Importar AttachmentManager de ./attachments.js
   - Exportar funcion `openTicketModal(cardData)` donde cardData = { title, transcript, bullets, cardElement }
   - Al abrir:
     - Mostrar modal (remove hidden)
     - Prellenar titulo con cardData.title (editable)
     - Prellenar descripcion con cardData.bullets formateados como lista con guiones
     - Instanciar AttachmentManager en el container de previews
     - Conectar botones "Anadir archivo" y "Hacer foto" a AttachmentManager.addFiles()
     - Si addFiles devuelve error, mostrar el error como texto rojo debajo de la seccion adjuntos
   - Al cerrar (boton X o Cancelar): ocultar modal, limpiar AttachmentManager, resetear formulario
   - Al submit ("Crear Ticket"):
     a. Deshabilitar boton submit, mostrar progress bar al 10%
     b. Leer valores del formulario
     c. Construir markdown_description: bullets como lista markdown + transcripcion completa al final
     d. POST /api/ticket con { name: titulo, markdown_description, reportedBy, affectedUser, projectId, assetId }
     e. Progress bar al 50%
     f. Si hay archivos: POST /api/attachment con FormData (taskId + archivos)
     g. Progress bar al 90%
     h. Si todo ok:
        - Progress bar al 100%
        - Cerrar modal
        - Actualizar la card original: cambiar badge a "Enviada" (badge-sent, verde), anadir enlace al ticket
        - showToast con mensaje de exito y enlace (usar innerHTML del toast para incluir link)
     i. Si el ticket se creo pero adjuntos fallaron:
        - Mostrar error dentro del modal: "Ticket creado pero X adjuntos fallaron"
        - Boton "Reintentar adjuntos" que solo reintenta la subida con el taskId ya obtenido
        - La card se marca como enviada igualmente (el ticket existe)
     j. Si el ticket fallo: mostrar error en el modal, rehabilitar boton submit
   - Click fuera del modal (en overlay) cierra el modal
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/_ALFRED/Bugshot && grep -q "ticket-modal" src/index.html && grep -q "openTicketModal" src/js/ticket-modal.js && grep -q "AttachmentManager" src/js/ticket-modal.js && grep -q "badge-sent" src/css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Modal completo con formulario, campos editables/prellenados, seccion de adjuntos con preview, progress bar, y logica de envio con manejo de errores parciales</done>
</task>

<task type="auto">
  <name>Task 3: Conectar boton "Crear Ticket" en cards al modal</name>
  <files>src/js/app.js</files>
  <action>
1. En `src/js/app.js`:
   - Importar `openTicketModal` de `./ticket-modal.js`
   - En la funcion `runSummarize`, dentro del .then (donde se crea el boton "Crear Ticket"):
     - Quitar el `disabled` del boton
     - Anadir event listener click que:
       a. Lee card.dataset.summaryTitle, card.dataset.summaryTranscript, card.dataset.summaryBullets (JSON.parse)
       b. Llama openTicketModal({ title, transcript, bullets, cardElement: card })
   - El boton debe quedar deshabilitado si la card ya fue enviada (tiene badge-sent)

2. Verificar que el flujo completo funciona:
   - Grabar audio -> transcribir -> resumir -> click "Crear Ticket" -> modal se abre con datos prellenados
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/_ALFRED/Bugshot && grep -q "openTicketModal" src/js/app.js && grep -q "disabled" src/js/app.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Boton "Crear Ticket" activo en cards completadas, abre modal con datos del resumen prellenados. Cards enviadas muestran badge verde y boton deshabilitado.</done>
</task>

</tasks>

<checkpoint type="checkpoint:human-verify" gate="blocking">
  <what-built>Flujo completo de creacion de ticket: modal con campos editables, adjuntos con preview, envio a ClickUp con progress bar y feedback</what-built>
  <how-to-verify>
    1. Ejecutar `bun run server/index.js`
    2. Abrir http://localhost:3000 en Chrome
    3. Grabar audio describiendo un bug (minimo 5 segundos)
    4. Esperar a que aparezca el resumen con titulo y bullets
    5. Pulsar "Crear Ticket" — verificar que el modal se abre con titulo editable y descripcion prellenada
    6. Modificar el titulo (verificar RESU-03)
    7. Anadir una foto desde galeria — verificar preview con thumbnail
    8. Intentar anadir mas de 5 archivos — verificar que muestra error
    9. Rellenar "Reportado por" y "Project ID"
    10. Pulsar "Crear Ticket" — verificar progress bar y confirmacion con enlace
    11. Verificar que la card muestra badge "Enviada" en verde
    12. Verificar que el ticket existe en ClickUp con titulo, descripcion y adjunto
  </how-to-verify>
  <resume-signal>Escribir "approved" o describir problemas encontrados</resume-signal>
</checkpoint>

<verification>
- El modal se abre con datos prellenados del resumen de Claude
- El titulo es editable, la descripcion no
- Adjuntos se pueden anadir y quitar con preview
- Validaciones de limites funcionan (5 archivos, 10MB)
- Progress bar visible durante envio
- Toast de confirmacion con enlace al ticket
- Card marcada como enviada con badge verde
- Error parcial de adjuntos se maneja correctamente
</verification>

<success_criteria>
Flujo completo audio-to-ticket funcionando: grabar -> resumir -> editar titulo -> adjuntar archivos -> crear ticket en ClickUp -> ver confirmacion con enlace. La card refleja el estado de envio.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tickets-adjuntos/04-02-SUMMARY.md`
</output>
