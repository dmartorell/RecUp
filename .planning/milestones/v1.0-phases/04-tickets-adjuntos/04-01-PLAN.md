---
phase: 04-tickets-adjuntos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/ticket.js
  - server/routes/attachment.js
  - server/index.js
  - package.json
autonomous: true
requirements: [TICK-01, ADJU-03]

must_haves:
  truths:
    - "POST /api/ticket crea una tarea en ClickUp y devuelve id + url"
    - "POST /api/attachment sube archivos a un ticket existente en ClickUp"
    - "Las credenciales de ClickUp nunca se exponen al frontend"
  artifacts:
    - path: "server/routes/ticket.js"
      provides: "Proxy endpoint para crear tareas en ClickUp"
      exports: ["default (Router)"]
    - path: "server/routes/attachment.js"
      provides: "Proxy endpoint para subir adjuntos a ClickUp"
      exports: ["default (Router)"]
  key_links:
    - from: "server/index.js"
      to: "server/routes/ticket.js"
      via: "import + app.use"
      pattern: "import.*ticket.*use.*ticket"
    - from: "server/index.js"
      to: "server/routes/attachment.js"
      via: "import + app.use"
      pattern: "import.*attachment.*use.*attachment"
    - from: "server/routes/ticket.js"
      to: "https://api.clickup.com/api/v2/list/{id}/task"
      via: "fetch POST con Authorization header"
      pattern: "api\\.clickup\\.com.*list.*task"
    - from: "server/routes/attachment.js"
      to: "https://api.clickup.com/api/v2/task/{id}/attachment"
      via: "fetch POST multipart con Authorization header"
      pattern: "api\\.clickup\\.com.*task.*attachment"
---

<objective>
Endpoints backend proxy para ClickUp: crear tareas y subir adjuntos.

Purpose: El frontend necesita dos endpoints que oculten las credenciales de ClickUp y hagan proxy de las llamadas API. Esto es la base sobre la que se construye el modal de creacion de tickets.
Output: Dos rutas Express funcionales registradas en el servidor.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tickets-adjuntos/04-RESEARCH.md

<interfaces>
<!-- Patron establecido en server/routes/summarize.js -->
From server/routes/summarize.js:
```javascript
import { Router } from 'express';
const router = Router();
router.post('/api/summarize', async (req, res) => { ... });
export default router;
```

From server/index.js:
```javascript
import summarizeRouter from './routes/summarize.js';
app.use(express.json());
app.use(summarizeRouter);
```

From .env (ya configurado):
```
CLICKUP_API_KEY=...
CLICKUP_LIST_ID=901507966221
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar multer y crear endpoint POST /api/ticket</name>
  <files>package.json, server/routes/ticket.js, server/index.js</files>
  <action>
1. Ejecutar `bun add multer`

2. Crear `server/routes/ticket.js` siguiendo el patron de summarize.js:
   - Router con POST /api/ticket
   - Recibe JSON body con: `name` (string, obligatorio), `markdown_description` (string), `reportedBy` (string), `affectedUser` (string), `projectId` (string), `assetId` (string)
   - Validar que `name` no este vacio, devolver 400 si lo esta
   - Verificar que CLICKUP_API_KEY y CLICKUP_LIST_ID existan en env, devolver 500 si no
   - Construir `markdown_description` final: concatenar la descripcion de Claude + metadata (reportedBy, affectedUser, projectId, assetId) como seccion al final del markdown. Formato:
     ```
     {descripcion_claude}

     ---
     **Reportado por:** {reportedBy}
     **Usuario afectado:** {affectedUser}
     **Project ID:** {projectId}
     **Asset ID:** {assetId}
     ```
   - Hacer fetch POST a `https://api.clickup.com/api/v2/list/${CLICKUP_LIST_ID}/task` con headers Authorization y Content-Type application/json
   - Body a ClickUp: `{ name, markdown_description, priority: 3, status: "to do" }`
   - Si ClickUp responde ok: devolver `{ id: data.id, url: data.url }`
   - Si ClickUp responde error: devolver el status code de ClickUp con `{ error: data }`

3. En `server/index.js`: importar ticketRouter y registrarlo con `app.use(ticketRouter)`. Anadir warning si falta CLICKUP_API_KEY o CLICKUP_LIST_ID.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/_ALFRED/Bugshot && bun run server/index.js &amp; sleep 1 &amp; curl -s -X POST http://localhost:3000/api/ticket -H "Content-Type: application/json" -d '{"name":""}' | grep -q "error" &amp;&amp; echo "PASS: empty name rejected" || echo "FAIL"; kill %1 2>/dev/null</automated>
  </verify>
  <done>POST /api/ticket valida input, hace proxy a ClickUp API con credenciales del servidor, y devuelve id + url del ticket creado</done>
</task>

<task type="auto">
  <name>Task 2: Crear endpoint POST /api/attachment con multer</name>
  <files>server/routes/attachment.js, server/index.js</files>
  <action>
1. Crear `server/routes/attachment.js`:
   - Router con POST /api/attachment
   - Configurar multer con memoryStorage y limite de 100MB por archivo, maximo 5 archivos
   - Middleware: `upload.array('attachment', 5)`
   - Recibe `taskId` en req.body (viene como campo del FormData)
   - Validar que taskId exista y que haya al menos 1 archivo, devolver 400 si no
   - Verificar CLICKUP_API_KEY en env
   - Para CADA archivo en req.files (secuencialmente, NO en paralelo — evitar rate limiting):
     - Crear FormData nativo
     - Append `attachment` como `new Blob([file.buffer], { type: file.mimetype })` con filename `file.originalname`
     - IMPORTANTE: NO setear Content-Type manualmente en el fetch — fetch lo genera automaticamente con el boundary
     - Fetch POST a `https://api.clickup.com/api/v2/task/${taskId}/attachment` con solo header Authorization
     - Si falla un archivo: devolver error con `{ error: data, file: file.originalname, uploaded: results }` para que el frontend sepa cuales se subieron
     - Si ok: acumular en results
   - Devolver `{ attachments: results }`

2. En `server/index.js`: importar attachmentRouter y registrarlo con `app.use(attachmentRouter)`.
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/_ALFRED/Bugshot && grep -q "multer" package.json && grep -q "attachmentRouter" server/index.js && grep -q "memoryStorage" server/routes/attachment.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>POST /api/attachment parsea multipart con multer, reenvia cada archivo secuencialmente a ClickUp API, y devuelve array de resultados</done>
</task>

</tasks>

<verification>
- `bun run server/index.js` arranca sin errores
- POST /api/ticket con body vacio devuelve 400
- POST /api/ticket con name valido llama a ClickUp (verificable con CLICKUP_API_KEY real)
- POST /api/attachment sin taskId devuelve 400
- grep confirma que las credenciales se leen de process.env, nunca hardcodeadas
</verification>

<success_criteria>
Dos endpoints proxy funcionales que ocultan credenciales y hacen proxy a ClickUp API v2. El servidor arranca con las 3 rutas registradas (summarize + ticket + attachment).
</success_criteria>

<output>
After completion, create `.planning/phases/04-tickets-adjuntos/04-01-SUMMARY.md`
</output>
