---
phase: 03-resumen-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/index.js
  - server/routes/summarize.js
  - src/js/summarizer.js
  - src/js/app.js
  - src/css/styles.css
  - .env
autonomous: true
requirements: [RESU-01, RESU-02, RESU-03]
user_setup:
  - service: anthropic
    why: "Proxy a Claude API para generar resumenes"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/ -> API Keys -> Create Key"

must_haves:
  truths:
    - "Al parar una grabacion, la card aparece con transcripcion cruda y badge Processing con spinner"
    - "Tras 1-2s, la card se actualiza con titulo, transcripcion limpia y bullets generados por Claude"
    - "El badge cambia de Processing a Completed cuando Claude responde"
    - "Si Claude falla, la card muestra badge Error y boton Reintentar que mantiene la transcripcion cruda"
    - "Si la transcripcion esta vacia, la card muestra 'No se detecto voz' sin llamar a Claude"
    - "La API key de Anthropic nunca se expone en el frontend"
    - "El boton Crear Ticket aparece visible pero deshabilitado"
  artifacts:
    - path: "server/routes/summarize.js"
      provides: "POST /api/summarize proxy a Claude Messages API"
      exports: ["default (Router)"]
    - path: "src/js/summarizer.js"
      provides: "Modulo frontend que llama al endpoint proxy"
      exports: ["summarize"]
    - path: "src/js/app.js"
      provides: "createCard actualizado con estados y flujo de resumen"
    - path: "src/css/styles.css"
      provides: "Estilos para badges, spinner, bullets, boton ticket"
  key_links:
    - from: "src/js/app.js"
      to: "src/js/summarizer.js"
      via: "import { summarize }"
      pattern: "import.*summarizer"
    - from: "src/js/summarizer.js"
      to: "/api/summarize"
      via: "fetch POST"
      pattern: "fetch.*api/summarize"
    - from: "server/routes/summarize.js"
      to: "https://api.anthropic.com/v1/messages"
      via: "fetch POST con x-api-key"
      pattern: "fetch.*api.anthropic.com"
    - from: "server/index.js"
      to: "server/routes/summarize.js"
      via: "import + app.use"
      pattern: "import.*summarize"
---

<objective>
Endpoint proxy a Claude API + cards con estados de resumen IA

Purpose: Convertir la transcripcion cruda en titulo + transcripcion limpia + bullets estructurados usando Claude Haiku, mostrando el resultado en las cards del feed con estados visuales (Processing/Completed/Error).

Output: Ruta POST /api/summarize funcional, modulo summarizer.js frontend, cards actualizadas con badges, spinner, bullets y boton "Crear Ticket" deshabilitado.
</objective>

<execution_context>
@/Users/danielmartorell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielmartorell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-resumen-ia/03-CONTEXT.md
@.planning/phases/03-resumen-ia/03-RESEARCH.md

<interfaces>
<!-- Codigo existente relevante -->

From server/index.js:
```javascript
import 'dotenv/config';
import express from 'express';
// app.use(express.json()) ya configurado
// app.use(express.static(...)) sirve src/
```

From src/js/app.js:
```javascript
// createCard(transcript, audioBlob, duration) — se debe extender
// showToast(message) — reutilizable para errores
// formatDuration(ms) — ya implementado
// feed.prepend(card) — patron de insercion
```

From .env:
```
ANTHROPIC_API_KEY=  # Renombrar CLAUDE_API_KEY a ANTHROPIC_API_KEY
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint POST /api/summarize y modulo frontend summarizer</name>
  <files>server/routes/summarize.js, server/index.js, src/js/summarizer.js, .env</files>
  <action>
1. Renombrar `CLAUDE_API_KEY` a `ANTHROPIC_API_KEY` en .env (mantener el valor vacio, el usuario lo configura).

2. Crear `server/routes/summarize.js`:
   - Router Express con POST /api/summarize
   - Recibe `{ transcript }` en body
   - Valida que transcript no este vacio (400 si lo esta)
   - Verifica que ANTHROPIC_API_KEY existe en env (500 con mensaje claro si no)
   - Llama a `https://api.anthropic.com/v1/messages` con fetch nativo:
     - Headers: `x-api-key`, `anthropic-version: 2023-06-01`, `content-type: application/json`
     - Body: model `claude-haiku-4-5`, max_tokens 1024, temperature 0.3
     - System prompt en espanol, tono tecnico conciso, genera JSON `{ title, transcript, bullets[] }`
     - System prompt debe instruir: "Responde UNICAMENTE con JSON valido, sin texto adicional, sin markdown code blocks"
   - Usar AbortController con timeout de 30s
   - Parsear response.content[0].text con JSON.parse en try/catch
   - Si JSON invalido: devolver 502 con error descriptivo
   - Si API devuelve error HTTP: propagar status y mensaje
   - Devolver el JSON parseado `{ title, transcript, bullets }` al frontend

3. Actualizar `server/index.js`:
   - Importar el router de summarize
   - Registrar con `app.use(summarizeRouter)` antes del listen
   - Anadir warning al arrancar si ANTHROPIC_API_KEY no esta configurada

4. Crear `src/js/summarizer.js`:
   - Exportar funcion async `summarize(transcript)` que hace POST a /api/summarize
   - Envia `{ transcript }` como JSON
   - Si response no es ok: leer error del body y lanzar Error con el mensaje
   - Si ok: devolver el JSON parseado `{ title, transcript, bullets }`
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/_ALFRED/Bugshot && bun run server/index.js &amp; sleep 1 && curl -s -X POST http://localhost:3000/api/summarize -H "Content-Type: application/json" -d '{}' | grep -q "error" && curl -s -X POST http://localhost:3000/api/summarize -H "Content-Type: application/json" -d '{"transcript":"test"}' ; kill %1 2>/dev/null</automated>
  </verify>
  <done>POST /api/summarize valida input vacio (400), responde con error si no hay API key, y llama a Claude correctamente cuando hay key y transcript. Modulo summarizer.js exporta funcion summarize().</done>
</task>

<task type="auto">
  <name>Task 2: Cards con estados Processing/Completed/Error, badges, bullets y boton Crear Ticket</name>
  <files>src/js/app.js, src/css/styles.css</files>
  <action>
1. Actualizar `src/css/styles.css` con estilos nuevos:
   - `.badge`: inline-flex, padding 2px 8px, border-radius 9999px, font-size 0.75rem, font-weight 500
   - `.badge-audio`: bg azul claro (#dbeafe), texto azul (#1d4ed8)
   - `.badge-processing`: bg amarillo claro (#fef3c7), texto amarillo oscuro (#92400e)
   - `.badge-completed`: bg verde claro (#d1fae5), texto verde oscuro (#065f46)
   - `.badge-error`: bg rojo claro (#fee2e2), texto rojo (#991b1b)
   - `.card-badges`: display flex, gap 6px, align-items center
   - `.card-spinner`: CSS spinner con @keyframes spin (border 3px, border-top color accent, 20px tamano, margin 12px auto)
   - `.card-title`: font-size 1.1rem, font-weight 600, color gray-900, margin-bottom 4px
   - `.card-bullets`: list-style disc, padding-left 20px, margin-top 8px, color gray-700, font-size 0.95rem, line-height 1.6
   - `.card-bullets li`: margin-bottom 4px
   - `.card-footer`: margin-top 12px, padding-top 12px, border-top 1px solid #e5e7eb
   - `.btn-create-ticket`: bg accent (#2563eb), text white, padding 8px 16px, border-radius 8px, font-weight 500, font-size 0.875rem, cursor pointer
   - `.btn-create-ticket:disabled`: opacity 0.5, cursor not-allowed
   - `.btn-retry`: bg transparent, border 1px solid #d1d5db, padding 6px 12px, border-radius 6px, font-size 0.8rem, cursor pointer, color gray-600

2. Actualizar `src/js/app.js`:
   - Importar `{ summarize }` de `./summarizer.js`
   - Modificar `createCard(transcript, audioBlob, duration)`:
     a. Si transcript esta vacio o solo espacios: crear card con mensaje "No se detecto voz. Intenta grabar de nuevo." sin spinner, sin llamar a Claude, badge "Audio" solamente. Return temprano.
     b. Si hay transcript: crear card con estructura nueva:
        - Header: div.card-badges con badge "Audio" + badge "Processing", mas card-time y card-delete
        - Body: p.card-text con transcripcion cruda + div.card-spinner
        - Sin footer aun (aparece al completar)
     c. Guardar referencia al card DOM element
     d. Llamar a `summarize(transcript)` de forma async (no await en createCard, usar .then/.catch o IIFE async)
     e. En .then (exito):
        - Actualizar badge: quitar "Processing", poner "Completed"
        - Quitar spinner
        - Anadir h3.card-title con result.title
        - Reemplazar p.card-text con result.transcript (transcripcion limpia)
        - Anadir ul.card-bullets con result.bullets.map a li
        - Anadir div.card-footer con boton "Crear Ticket" deshabilitado (disabled)
        - Guardar datos del resumen en card.dataset (title, transcript limpia, bullets como JSON) para fase 4
     f. En .catch (error):
        - Actualizar badge: quitar "Processing", poner "Error"
        - Quitar spinner
        - Mantener transcripcion cruda visible
        - Anadir boton "Reintentar" que al hacer click repite el flujo (quita Error, pone Processing, llama summarize otra vez)
        - Mostrar toast con el error
   - Guardar audioBlob en card.dataset como antes (audioBlobUrl)
  </action>
  <verify>
    <automated>cd /Volumes/T7_SAMSUNG/_ALFRED/Bugshot && grep -q "summarize" src/js/app.js && grep -q "badge" src/css/styles.css && grep -q "card-spinner" src/css/styles.css && grep -q "btn-create-ticket" src/css/styles.css && echo "OK"</automated>
  </verify>
  <done>Las cards muestran badge Audio + Processing con spinner al crearse. Al recibir respuesta de Claude: titulo, transcripcion limpia, bullets y badge Completed. En error: badge Error + boton Reintentar. Transcripcion vacia muestra mensaje sin llamar a Claude. Boton "Crear Ticket" visible pero deshabilitado.</done>
</task>

</tasks>

<verification>
1. Arrancar servidor con `bun run server/index.js`
2. Abrir http://localhost:3000 en Chrome
3. Grabar audio con voz -> al parar, card aparece con transcripcion cruda + badge Processing + spinner
4. Si ANTHROPIC_API_KEY esta configurada: card se actualiza con titulo, transcripcion limpia, bullets, badge Completed, boton "Crear Ticket" deshabilitado
5. Si ANTHROPIC_API_KEY no esta: card muestra badge Error + boton Reintentar + toast de error
6. Grabar sin hablar -> card muestra "No se detecto voz" sin spinner ni llamada a Claude
7. Verificar que la API key no aparece en ningun archivo frontend (solo en server/routes/summarize.js via process.env)
</verification>

<success_criteria>
- POST /api/summarize devuelve JSON `{ title, transcript, bullets[] }` desde Claude Haiku
- Cards muestran estados Processing -> Completed con transicion visual
- Error handling: badge Error + Reintentar cuando Claude falla
- Transcripcion vacia no genera llamada a Claude
- API key solo en servidor, nunca en frontend
- Boton "Crear Ticket" presente pero deshabilitado (se activa en fase 4)
</success_criteria>

<output>
After completion, create `.planning/phases/03-resumen-ia/03-01-SUMMARY.md`
</output>
